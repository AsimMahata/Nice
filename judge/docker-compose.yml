services:
  judge:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: nice-judge
    ports:
      - "5001:5001"
    volumes:
      - .:/app
      - /app/node_modules
    env_file:
      - .env
    environment:
      - NODE_ENV=development
